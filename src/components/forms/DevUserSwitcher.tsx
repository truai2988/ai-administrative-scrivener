'use client';

/**
 * DevUserSwitcher
 *
 * ⚠️ このコンポーネントは TEST_USERS 廃止に伴い無効化されました。
 * 実際のユーザー認証は useAuth() から取得した情報を使用します。
 *
 * @deprecated 削除予定 - 実装要件 Step 4 完了に伴い機能停止
 */

export function DevUserSwitcher() {
  // 本番実装では何も表示しない（ダミーユーザー切り替えUIは廃止）
  return null;
}
